/**
 * Imagen genérica de perfil. Se usa en todos los sitios donde puede faltar el
 * avatar (perfil, navbar, listado de usuarios) para no dejar el `<img>` roto.
 */
export const DEFAULT_AVATAR = 'assets/images/defaultUser.png';

/**
 * Imagen genérica de un ítem del catálogo (producto, hospedaje, pasadía). Se
 * usa como respaldo en las miniaturas de los listados, igual que
 * `DEFAULT_AVATAR` con las personas, para no dejar nunca un `<img>` roto ni una
 * celda vacía que no se distinga de "todavía cargando".
 */
export const DEFAULT_ITEM = 'assets/images/defaultItem.png';
